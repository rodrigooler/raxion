# Ops

Operational tooling and deployment configuration.

- `docker/anchor-devnet/`: pinned toolchain image for Solana/Anchor tasks.
- `config/deploy/`: deploy environment templates (no secrets committed).
